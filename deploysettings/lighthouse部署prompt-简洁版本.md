# 同步 fineSTEM 项目到 lighthouse 服务器

## 要求
- 使用 deploy_project_preparation 上传本地文件
- 只执行 docker-compose restart（不需要 build/down/up）
- 如果只改了 .env，只重启对应服务
- ❌ 严禁重建镜像、删除容器、修改 docker-compose.yml
- 验证服务健康状态

## 服务器
ap-beijing / lhins-2yr8snuu
