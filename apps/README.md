# Applications

此目录包含 fineSTEM 项目的所有独立应用程序。

## 目录结构

- **[public-web](./public-web/)**: 面向公众的 Web 应用，目前包含 fineSTEM MVP 1.0 的核心功能。

## 开发说明

本项目采用 Monorepo 风格的目录结构，不同的应用位于 `apps/` 下的独立子目录中。
目前核心开发工作集中在 `public-web` 下的 MVP Phase 1 阶段。
