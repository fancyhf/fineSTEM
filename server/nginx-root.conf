server {
    listen 80;
    server_name _;

    # 根路径重定向到项目选择页面
    location = / {
        rewrite ^(.*)$ /project-list break;
        return 301 /project-list;
    }

    # 项目选择页面（重定向后的目标）
    location = /project-list {
        default_type text/html;
        return 200 '<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>项目列表 - Lighthouse Server</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #fff;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            width: 100%;
        }
        .header {
            text-align: center;
            margin-bottom: 40px;
        }
        .header h1 {
            font-size: 48px;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }
        .header p {
            font-size: 18px;
            opacity: 0.9;
        }
        .project-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        .project-card {
            background: rgba(255,255,255,255,0.1);
            border-radius: 12px;
            padding: 30px;
            transition: all 0.3s ease;
            text-decoration: none;
            color: #fff;
            border: 2px solid rgba(255,255,255,255,0.2);
        }
        .project-card:hover {
            transform: translateY(-5px);
            background: rgba(255,255,255,255,0.2);
            border-color: rgba(255,255,255,255,0.4);
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }
        .project-card h2 {
            font-size: 24px;
            margin-bottom: 10px;
            color: #64b5f6;
        }
        .project-card .path {
            font-size: 14px;
            opacity: 0.8;
            margin-bottom: 5px;
            font-family: "Courier New", monospace;
        }
        .project-card .url {
            font-size: 14px;
            opacity: 0.7;
        }
        .status {
            margin-top: 40px;
            padding: 20px;
            background: rgba(0,0,0,0.2);
            border-radius: 8px;
            text-align: center;
        }
        .footer {
            margin-top: auto;
            padding: 20px;
            text-align: center;
            opacity: 0.6;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚀 Lighthouse Server</h1>
            <p>选择要访问的项目</p>
        </div>
        
        <div class="project-grid">
            <a href="/finestem" class="project-card">
                <h2>📚 fineSTEM</h2>
                <div class="path">/finestem</div>
                <div class="url">http://43.140.204.127:8080/fineSTEM</div>
            </a>
            
            <a href="/project2" class="project-card">
                <h2>🎨 Project 2</h2>
                <div class="path">/project2</div>
                <div class="url">http://43.140.204.127:8081/project2</div>
            </a>
            
            <a href="/project3" class="project-card">
                <h2>⚡ Project 3</h2>
                <div class="path">/project3</div>
                <div class="url">http://43.140.204.127:8082/project3</div>
            </a>
        </div>
        
        <div class="status">
            <p><strong>服务器状态：</strong>运行正常</p>
            <p>当前部署了 3 个项目</p>
        </div>
        
        <div class="footer">
            <p>Tencent Lighthouse · 43.140.204.127</p>
        </div>
    </div>
</body>
</html>';
    }

    # fineSTEM 项目
    location /finestem {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # project2 项目（示例）
    location /project2 {
        proxy_pass http://localhost:8081;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # project3 项目（示例）
    location /project3 {
        proxy_pass http://localhost:8082;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
