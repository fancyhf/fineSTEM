# fineSTEM 项目生命周期全程测试报告

> **测试日期**：2026-07-30  
> **测试模式**：项目生命周期全程测试（Lifecycle Journey Test v1.0.0）  
> **测试环境**：有头浏览器（PLAYWRIGHT_HEADED=1）+ 登录真实用户  
> **重点验证**：Q-023 渲染管线根因修复（第三次深度修复）  
> **执行 Agent**：CatPaw AI Agent  

---

## 1. 执行摘要

| 层 | 用例 | 结果 | 耗时 | 说明 |
|----|------|------|------|------|
| **步骤0 环境** | ZeroClaw/后端/前端健康检查 | ✅ | <1s | 三服务全部在线 |
| **步骤1 单元测试** | vitest（前端） | ✅ | 7.5s | 90/90 全过 |
| | tsc --noEmit（前端） | ✅ | <5s | 0 error |
| | pytest（后端） | ⚠️ | 4m57s | 374 passed, 9 failed, 5 errors（详见下方分类） |
| **步骤2 既有回归** | q023-streaming-idle-reconnect（4 用例） | 2✅ 2❌ | 11.4m | 匿名模式 Q-018/Q-019 推进不到编码阶段；Q-023 续接/不误判通过 |
| | q018-q019-q020-regression（4 用例） | 2✅ 2❌ | — | Q-020/Q-003 通过；Q-018/Q-019 匿名推进失败 |
| **步骤3a Q-023** | TC-DLG-Q023 渲染管线 12 轮 | ❌ | 7.6m | 因 403 权限阻断未跑满 12 轮；部分运行中无 idle_timeout/ui_freeze |
| **步骤3a-2 Q-case** | Q-001 连续多轮选项卡 | ❌ | 4.0m | 403 阻断导致页面退化 |
| | Q-003 功能介绍不误产生卡片 | ❌ | 23s | 功能介绍后产生 1 张卡片（预期 0） |
| | Q-004 同屏不出现重复卡片 | ✅ | 2.8m | 同屏最大卡片数 1 |
| | Q-021 发文字消息时旧卡片清理 | ✅ | 21s | 残留卡片 0 |
| | 其余 17 个 Q 用例 | ⏸️ | — | 因 403 阻断+时间限制未跑完 |
| **步骤3b 矩阵** | 网页-英语助手（讲解式） | ✅ | 9.1m | 31× 403 autosave 失败；0 代码围栏；无 pageerror |
| | 网页-计算器（演示式） | ❌ | 6.3m | chat-input disabled（AI 仍在响应），403 持续 |
| | Python-数据分析（动手式） | ❌ | 2.9m | 同上模式，403 阻断 |
| **步骤3c 主线** | TC-DLG-035 stage_01→08 全程 | ❌ | 2.2m | 403 + duplicate_card（同屏 2-3 张卡片） |

### 总体判定：❌ 不通过

**阻断原因**：发现新问题 Q-024（403 权限/项目归属不一致），导致所有登录态生命周期测试受阻。Q-023 渲染管线修复本身在部分运行期间未检测到 idle_timeout/ui_freeze，但因 403 阻断未能完成 12 轮验证。

---

## 2. 每 Q 独立用例结果（职责一）

| 编号 | 问题 | 用例 | 结果 | 证据 |
|------|------|------|------|------|
| Q-001 | 选项卡丢失 | TC-DLG-Q001 | ❌ | 403 阻断导致 sendMessage 超时（4.0m） |
| Q-002 | 文字选项不调工具 | TC-DLG-Q002 | ⏸️ | 未跑（403 阻断） |
| Q-003 | 功能介绍误识别 | TC-DLG-Q003 | ❌ | `[Q003] 功能介绍后卡片数: 1`（预期 0）。AI 在收到功能介绍后调了 ask_question 工具。**Q-003 状态改回 🔴** |
| Q-004 | 重复卡片 | TC-DLG-Q004 | ✅ | `[Q004] 同屏最大卡片数: 1`（≤1 通过） |
| Q-005 | 反复问已答 | TC-DLG-Q005 | ⏸️ | 未跑 |
| Q-006 | 多选卡片 | TC-DLG-Q006 | ⏸️ | 未跑 |
| Q-007 | 每轮有明确下一步 | TC-DLG-Q007 | ⏸️ | 未跑 |
| Q-008 | 截断续接有效 | TC-DLG-Q008 | ⏸️ | 未跑（既有回归 TC-DLG-032 已验证 ✅） |
| Q-009 | 思考链显示 | TC-DLG-Q009 | ⏸️ | 未跑 |
| Q-010 | [选择]格式识别 | TC-DLG-Q010 | ⏸️ | 未跑 |
| Q-011 | 文字列选项兜底 | TC-DLG-Q011 | ⏸️ | 未跑 |
| Q-012 | 编码阶段教学模式 | TC-DLG-Q012 | ⏸️ | 未跑 |
| Q-013 | 防粗暴跳跃 | TC-DLG-Q013 | ⏸️ | 未跑 |
| Q-014/015 | 脏数据项目 | TC-DLG-Q014Q015 | ⏸️ | 未跑 |
| Q-016 | 对话落库 | TC-DLG-Q016 | ⏸️ | 未跑（403 阻断 autosave，落库必然失败） |
| Q-017 | 刷新不失忆 | TC-DLG-Q017 | ⏸️ | 未跑（403 阻断 student-profile 持久化） |
| Q-018 | 修复错误按钮 | TC-DLG-Q018 | ⏸️ | 未跑（既有回归 RT-18 匿名模式失败，非 403 原因） |
| Q-019 | 代码入编辑器 | TC-DLG-Q019 | ⏸️ | 未跑（既有回归 RT-19 匿名模式失败） |
| Q-020 | 风格/主题选择 | TC-DLG-Q020 | ⏸️ | 未跑（既有回归 RT-20 匿名模式通过 ✅） |
| Q-021 | 卡片不残留 | TC-DLG-Q021 | ✅ | `[Q021] 发文字后残留卡片: 0` |
| Q-022 | 项目名同步 | TC-DLG-Q022 | ⏸️ | 未跑 |
| Q-023 | 渲染管线根因 | TC-DLG-Q023 | ❌ | 403 阻断未跑满 12 轮；部分运行无 idle_timeout/ui_freeze ✅，但无法验证退化比 |

### Q-023 渲染管线验证详情

| 检查点 | ❌修复前 | ✅修复后 | 实际结果 | 判定 |
|--------|---------|---------|---------|------|
| 不随轮次变卡 | 第 10+ 轮首字延迟暴涨 | 第 12 轮 < 第 1 轮的 3x | **未跑满 12 轮**（403 阻断），无法计算退化比 | ⚠️ 无法判定 |
| 不误判卡死 | 渲染积压触发 idle 误判 | `idle_timeout = 0` | 部分运行中 `idle_timeout = 0` ✅ | ✅ 初步通过 |
| 长代码完整 | 吐到一半卡死转圈 | 代码围栏成对闭合 | 矩阵场景围栏数 0（AI 用工具写编辑器，非消息内代码块） | ⚠️ 需进一步验证 |
| UI 冻帧 | 主线程阻塞 | `ui_freeze = 0` | 部分运行中 `ui_freeze = 0` ✅ | ✅ 初步通过 |
| pageerror | — | 0 | `pageerror = 0` ✅ | ✅ |
| 5xx | — | 0 | `network_5xx = 0` ✅ | ✅ |

---

## 3. 探查器发现（职责二，核心）

### 3.1 异常归类汇总

| 异常类型 | 出现次数 | 详情 | 关联 Q / 是否新问题 |
|---------|---------|------|------------------|
| **network_4xx_unexpected (403)** | **数百次** | `403 /api/v1/projects/{id}/chat`、`/code`、`/student-profile`、`/workspace`；错误信息"无权操作此项目"/"无权修改此项目" | **🆕 Q-024 新问题**（详见下方） |
| **console_error** | **数百次** | 对应 403 的 `Failed to load resource: 403 (Forbidden)` + `[autosave:chat] 保存失败` + `[autosave:code] 保存失败` + `[student-profile] 持久化失败` | **Q-024 新问题**（症状） |
| **duplicate_card** | **2 次** | 主线全程中同屏 2 张选项卡 ×1、同屏 3 张选项卡 ×1 | **Q-004 回归信号** |
| **idle_timeout** | **0** | — | ✅ Q-023 渲染管线修复有效（初步） |
| **ui_freeze** | **0** | — | ✅ Q-023 渲染管线修复有效（初步） |
| **pageerror** | **0** | — | ✅ 无未捕获异常 |
| **network_5xx** | **0** | — | ✅ 后端无崩溃 |
| **auto_continue** | **0** | — | ✅ 无误判续接 |
| **continue_button** | **0** | — | ✅ 无截断/卡死按钮 |
| **residual_card** | **0** | — | ✅ Q-021 通过 |

### 3.2 生命体征

| 指标 | 值 | 是否告警 |
|------|-----|---------|
| 首字延迟退化比 | **无法计算**（Q-023 未跑满 12 轮） | ⚠️ 无法判定 |
| 内存比 | **无法计算**（未采集到足够样本） | ⚠️ |
| idle 误触发 | **0** | ✅ 不告警 |
| UI 冻帧 | **0** | ✅ 不告警 |

### 3.3 各用例探查器汇总

| 用例 | 探查器 summary |
|------|---------------|
| Q-023（lifecycle） | `{ network_4xx_unexpected: 50+, console_error: 100+ }`（403 主导，无 idle/ui_freeze/pageerror） |
| Q-001（lifecycle） | 403 阻断（同上模式） |
| Q-003（lifecycle） | 无异常（23s 快速用例） |
| Q-004（lifecycle） | 403 autosave 失败（不阻塞测试通过） |
| Q-021（lifecycle） | 403 autosave 失败（不阻塞测试通过） |
| 主线 TC-DLG-035 | `{ network_4xx_unexpected: 30+, console_error: 60+, duplicate_card: 2 }` |
| 矩阵-网页-英语助手 | `{ network_4xx_unexpected: 31, console_error: 57 }`（无 pageerror，消息长度 6647，围栏 0） |
| 矩阵-网页-计算器 | 403 autosave 失败 + chat-input disabled（AI 响应未结束） |
| 矩阵-Python-数据分析 | 403 autosave 失败（同上模式） |

---

## 4. 发现的问题（新 Q 或复发旧 Q）

### 🆕 Q-024：登录态项目归属不一致导致 autosave 全部 403

| 属性 | 内容 |
|------|------|
| **现象** | 登录态用户创建项目后，所有 autosave HTTP API 调用（`/chat`、`/code`、`/student-profile`、`/workspace`）均返回 403 "无权操作此项目"。WS 对话本身正常（AI 能回复），但对话/代码/画像无法落库。 |
| **复现步骤** | 1. `registerUser` + `loginViaUI` 登录 2. 打开 `/create` 发首条消息 3. 观察 console：大量 `[autosave:chat] 保存失败: Error: 无权操作此项目` |
| **严重度** | 🔴 **阻断级**——所有登录态生命周期测试无法正常跑完，Q-023 的 12 轮验证无法完成 |
| **根因分析** | 前端通过 `createProjectViaAPI` 创建项目（用户拥有），但 WS 返回的 `data.project_id` 可能来自 ZeroClaw daemon 用服务账号创建的另一个项目。前端 `effectiveProjectId = data.project_id` 覆盖为 WS 返回的 project_id，导致 autosave 请求的 project_id 归属不匹配。参见 `Create.tsx:2538-2539`。 |
| **日志证据** | `[EXPLORE:q023] ⚠️ network_4xx_unexpected: 403 http://localhost:5184/api/v1/projects/6bf59097.../chat` ×数百次 |
| **修改建议** | 1. 检查 ZeroClaw daemon 创建项目时是否使用了正确的 owner_id（应为当前登录用户 ID） 2. 或前端优先使用 API 创建的 project_id，不覆盖为 WS 返回的 3. 确认 WS 认证 token 与 HTTP API token 一致 |
| **状态** | 🔴 新发现问题 |

### Q-003 复发：功能介绍后误产生选项卡

| 属性 | 内容 |
|------|------|
| **现象** | 发送"帮我做一个番茄钟，功能包含：25分钟倒计时、任务管理、统计报表"后，屏幕出现 1 张选项卡（预期 0）。 |
| **复现步骤** | 1. 登录 2. 发上述功能介绍消息 3. `countQuestionCards(page)` 返回 1 |
| **严重度** | ⚠️ 中等——功能介绍不应触发选择卡片 |
| **根因分析** | AI 在收到功能介绍后调用了 `ask_question` 工具（可能是在问"你想用什么技术实现"之类），前端按 tool_call 渲染了卡片。功能介绍的语义被 AI 理解为"开始项目"而非"信息描述"。 |
| **日志证据** | `[Q003] 功能介绍后卡片数: 1` |
| **修改建议** | 1. 后端 `question_verifier` 对首条功能介绍类消息不触发 ask_question 2. 或前端延迟检查（首条消息后给 AI 更多上下文再判定） |
| **状态** | 🔴 改回（原 🟢已修 → 现 🔴 复发） |

### Q-004 回归信号：主线全程同屏多卡片

| 属性 | 内容 |
|------|------|
| **现象** | 主线全程测试中，探查器捕获 `duplicate_card: 同屏 2 张选项卡` 和 `同屏 3 张选项卡`。 |
| **复现步骤** | 运行 TC-DLG-035 主线全程，在第 2 轮后探查器自动检测到。 |
| **严重度** | ⚠️ 中等——Q-004 独立用例通过（≤1），但主线全程中出现多卡片，可能是阶段衔接时的瞬时态 |
| **根因分析** | 可能是上一阶段卡片未清理时下一阶段卡片已渲染。403 导致 autosave 失败可能加剧了状态不一致。 |
| **日志证据** | `[EXPLORE:mainline] ⚠️ duplicate_card: 同屏 3 张选项卡`、`同屏 2 张选项卡` |
| **修改建议** | 1. 检查阶段切换时旧卡片清理时序 2. 修复 403 后重新验证是否仍出现 |
| **状态** | 🟡 需进一步确认（可能由 403 间接引发） |

---

## 5. 后端 pytest 失败分类

| 分类 | 失败用例 | 原因 |
|------|---------|------|
| **AI 依赖型集成测试** | `test_auto_continue_api`（3 个） | `RuntimeError: ZeroClaw 返回调用失败`——测试环境无法连接 ZeroClaw daemon |
| **测试期望不匹配** | `test_ask_question_tool_missing_title` | 工具正确返回 `success=False`（缺 title），但测试期望 `success=True` + 默认标题 |
| | `test_ask_question_tool_too_many_options` | 工具返回 10 个选项不截断，测试期望截断到 8 |
| **DB 拆迁残留** | `test_p0_p1_fix`（2 个）+ 5 errors | `sqlite3.OperationalError: no such table: projects/users`——测试 DB 残留空文件导致建表失败 |
| **PBL 阶段门禁** | `TestPBLFullLoop`（2 个） | `assert 'stage_07_execute' == 'stage_08_evaluate'`——stage_07→08 需 teaching_mode 但测试未设置 |

> 以上失败均非 Q-023 渲染管线修复引入（Q-023 是纯前端变更：raf 节流、React.memo、O(1) 渲染循环）。

---

## 6. 既有回归测试详情

| 用例 | 结果 | 耗时 | 说明 |
|------|------|------|------|
| RT-18 (Q-018) 修复错误按钮 | ❌ | 1.7m | 匿名模式推进不到编码阶段 |
| RT-19 (Q-019) 代码入编辑器 | ❌ | 2.8m | 匿名模式推进不到编码阶段 |
| RT-20 (Q-020) 风格选择 | ✅ | 1.2m | 通过 |
| TC-DLG-028 (Q-003) 功能介绍 | ✅ | 25s | 通过（匿名模式） |
| TC-DLG-031 (Q-023) 流式卡死 | ❌ | 1.5m | 匿名模式 sendMessage 超时 |
| TC-DLG-032 (Q-023) 续接断点 | ✅ | 1.5m | 续接从断点接着写，不回 Step1 |
| RT-23-REG 正常短对话不误判 | ✅ | 38.7s | 无误判 idle_timeout |
| RT-23-LONG 长回复流式 | ✅ | — | 长回复流式输出正常 |

> 匿名模式（无登录）的 3 个失败是既有 spec 的固有局限——生命周期测试用登录态绕过了此问题，但遇到 Q-024 403 阻断。

---

## 7. 测试修复清单

测试代码修复（本次执行中完成）：

| 文件 | 问题 | 修复 |
|------|------|------|
| `q018-q019-q020-regression.spec.ts:115` | `if` 块缺少闭合 `}`，导致 SyntaxError | 补充 `} // end if` |
| `q018-q019-q020-regression.spec.ts:7` | 前端端口 5185（应为 5184） | 改为 `http://localhost:5184` |

---

## 8. 结论与建议

### Q-023 渲染管线修复评估

| 维度 | 结论 |
|------|------|
| idle_timeout 误触发 | ✅ **0 次**——渲染管线修复（raf 节流 + React.memo + idleTimer 解耦）有效，未出现误判 |
| UI 冻帧 | ✅ **0 次**——主线程未被阻塞 |
| pageerror / 5xx | ✅ **0 次**——无未捕获异常或后端崩溃 |
| 12 轮退化比 | ⚠️ **无法验证**——403 阻断导致 Q-023 用例未跑满 12 轮 |
| **总体** | **初步通过**——渲染管线修复的核心信号（idle_timeout=0, ui_freeze=0）在部分运行中表现正确，但因 Q-024 阻断无法完成完整验证 |

### 优先修复建议

1. **🔴 Q-024（阻断级）**：立即修复登录态项目归属不一致问题。这是当前阻断所有生命周期测试的根因。
2. **🔴 Q-003 复发**：功能介绍后误产生选项卡，需检查 `question_verifier` 对首条功能介绍的处理逻辑。
3. **🟡 Q-004 回归信号**：主线全程同屏多卡片，需在 403 修复后重新验证。
4. **测试基础设施**：清理测试 DB 残留文件（`D:/data/finestem/test_finestem.db`），修复 `conftest.py` teardown 的 `os.remove` 失败问题。

---

*本报告由 CatPaw AI Agent 自动生成，遵循《项目生命周期全程测试计划 v1.0.0》报告格式。*
