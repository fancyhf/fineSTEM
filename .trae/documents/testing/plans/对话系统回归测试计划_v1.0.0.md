# 对话系统回归测试计划 v1.0.0

**版本**: v1.0.0
**日期**: 2026-07-23
**被测对象**: fineSTEM AI 对话系统（ZeroClaw + 前端 + MCP 工具）
**测试目标**: 验证问题清单（Q-001~Q-015）全部修复，无回归
**执行人**: 测试 agent（按 `测试工作指南_v1.0.0.md` 规范执行）
**依据**: `.trae/documents/问题清单_长期维护.md`

> **本计划是对话系统质量的最终把关**。覆盖选项卡渲染、多选、多卡、重复问、总结误识别、脏数据恢复等全部历史问题。
> **必须用 Playwright 有头测试推进到至少 stage_04**，才能覆盖全部场景。

---

## 0. 背景：为什么必须推进到 stage_04+

问题清单中的问题不是在单一阶段出现的——它们分布在不同阶段：

| 阶段 | 覆盖的问题 | 说明 |
|------|-----------|------|
| stage_00 | Q-001 丢卡、Q-002 工具调用、Q-010 [选择] 格式 | 3 轮连续提问是丢卡的高发期 |
| stage_01 | Q-005 重复问、Q-006 多选 | 脑爆阶段有多选场景（兴趣爱好） |
| stage_02 | Q-007 只总结无下一步、Q-003 总结误识别 | 开题阶段 AI 倾向总结 |
| stage_03~04 | Q-004 重复卡 | 跨阶段推进时可能重复入栈 |
| 打开历史项目 | Q-014 非法 stages、Q-015 多层编码 | 脏数据导致 workspace 接口 500，AI 对话流不展现 |
| 刷新/重开项目 | Q-016 对话落库、Q-017 AI 失忆 | 刷新后对话丢失、AI 重复问已答问题 |

**只测前 2 轮（stage_00）无法发现 stage_02 的总结误识别和 stage_01 的多选问题。**

---

## 1. 强制要求

### 1.1 必须有头测试

```bash
# Playwright 必须用 --headed（有头浏览器），不用 headless
cd apps/frontend
RUN_AI_E2E=1 npx playwright test --project=chromium --headed --video=retain-on-failure --screenshot=on
```

**理由**：
- headless 模式下无法截图取证，出问题时无法复现
- 有头模式可以观察渲染时序，发现间歇性丢卡
- 录屏保留失败现场的完整交互过程

### 1.2 必须推进到 stage_04

测试脚本必须自动完成 stage_00 → stage_01 → stage_02 → stage_03 → stage_04 的完整流程。
每个阶段都要点选项卡回答，验证卡片渲染和多选。

### 1.3 必须对照问题清单

测试报告必须逐项回答 Q-001~Q-010 的状态（✅通过 / ❌复现 / ⚠️未覆盖）。

---

## 2. 测试用例清单

### 2.1 Playwright 有头 E2E（TC-DLG-001~010，核心验证）

> **这是最重要的测试组**。有头浏览器 + 录屏 + 截图，走完整 PBL 流程到 stage_04。

| 编号 | 用例 | 阶段 | 验证问题 | 通过标准 |
|------|------|------|---------|---------|
| TC-DLG-001 | stage_00 第 1 轮卡片渲染 | stage_00 | Q-001 | 发"我想做一个项目"→出现 `question-card`，含"年级"问题 |
| TC-DLG-002 | stage_00 连续 3 轮不丢卡 | stage_00 | Q-001 | 3 轮各有卡片，点选项后下一轮卡片正确出现 |
| TC-DLG-003 | stage_00 无重复卡 | stage_00 | Q-004 | 每轮屏幕上同一问题只有 1 张卡 |
| TC-DLG-004 | [选择] 格式正确识别 | stage_00 | Q-010 | 点选项后 AI 确认选择（不说"你没选"），推进到下一轮 |
| TC-DLG-005 | stage_01 多选卡片可多选 | stage_01 | Q-006 | 兴趣爱好卡片（multiple=true）能选 2+ 个选项 |
| TC-DLG-006 | stage_01 不重复问年级 | stage_01 | Q-005 | 进入脑爆后 AI 不再问"年级" |
| TC-DLG-007 | stage_02 总结不误产生卡片 | stage_02 | Q-003 | 发"总结一下进度"→**不产生** question-card |
| TC-DLG-008 | stage_02 有明确下一步 | stage_02 | Q-007 | 每轮 AI 回复结尾有 ask_question 或明确指引 |
| TC-DLG-009 | 推进到 stage_04 选轨道 | stage_04 | 全流程 | 能推进到技术轨道选择（5 选 1 卡片） |
| TC-DLG-010 | 全流程无 console error | 全程 | 健壮性 | 控制台无未捕获 JS 错误 |
| TC-DLG-011 | AI 文字选项列表兜底渲染 | stage_02+ | Q-011 | 当 AI 文字含选项列表但无 tool_call 时，前端 extractChoiceListStrict 兜底渲染卡片 |
| TC-DLG-012 | 总结文本不误兜底 | stage_02 | Q-003+Q-011 | AI 总结/进度编号列表不被误提取为卡片 |
| TC-DLG-026 | 打开含脏数据的历史项目不报 500 | 打开项目详情 | Q-014+Q-015 | 从 Research 页点"继续"进入 create，AI 对话流与代码正常展现，控制台无 `[restore] workspace 恢复失败` 或 `Internal Server Error` |
| TC-DLG-028 | 番茄钟功能介绍不误识别为选项卡 | stage_05+ | Q-003 | AI 用"包含：- 25分钟倒计时 - 待办任务增删改查"介绍功能时，前端**不**渲染选项卡；真问题（选轨道/教学模式）仍正常渲染 |
| TC-DLG-029 | 刷新页面后 AI 不失忆（核心） | 全流程 | Q-017 | 走完脑爆（选年级/兴趣/方向/选题）→ **刷新页面** → 继续对话 → AI 不重复问已答问题；`metadata.student_profile` 有值 |
| TC-DLG-030 | 对话确定项目名后项目区显示正确名字（核心） | 全流程 | Q-022 | 新建项目（默认长名字=首条消息截断）→ 脑爆/简报阶段 AI 确定项目名 → ① 侧边栏项目列表显示 AI 确认的短名字（不再显示"我想做一个..."截断）② 顶栏项目名也更新 ③ **刷新页面**后名字仍正确（projects.name 已自愈持久化） |
| TC-DLG-031 | 流式卡死保留内容 + 显示继续按钮 | stage_07 讲解式 | Q-023A | 让 AI 生成大段代码（讲解式 Step2）→ 若流式中途停顿（30s 无新内容）→ ① 已生成的"词库 ="等内容保留在聊天气泡（不丢）② 显示"继续生成"按钮（非干等 120s 转圈）③ 控制台有 `chunk 空闲超时` 日志 |
| TC-DLG-032 | 续接从断点接着写（不回 Step1） | stage_07 讲解式 | Q-023B | TC-DLG-031 卡死后点"继续生成" → ① AI 从"词库 ="接着写（非回到 Step1 重讲）② 对话历史/续接消息含 `<previous_output>` 上文 ③ 续接内容与前文格式连贯 |
| TC-DLG-033 | 大段代码完整输出不截断（核心） | stage_07 | Q-023A | 让 AI 生成完整 HTML+CSS+JS 项目代码（讲解式）→ ① 代码完整输出到 `</html>` 闭合（不在 CSS 中段截断）② 若被 max_tokens 截断，自动续接 1-3 次内补全 ③ 最终代码块 ``` 正确闭合、可运行 |
| TC-DLG-034 | 多次续接仍可继续（按钮不消失） | stage_07 | Q-023A | TC-DLG-033 若仍截断 → 点"继续生成" → ① 续接后若仍不完整，继续按钮**仍显示**（非被压制）② 可连续点 2-4 次 ③ 达 4 次上限后按钮隐藏（防死循环）④ 发新对话后计数重置、按钮恢复正常 |

### 2.2 单元测试（TC-DLG-011~022）

| 编号 | 用例 | 验证问题 | 通过标准 |
|------|------|---------|---------|
| TC-DLG-011 | extractChoiceListStrict 正例：选项列表提取 | Q-011 | 文字选项列表 → 生成 QuestionData |
| TC-DLG-012 | extractChoiceListStrict 反例：总结不误提取 | Q-003+Q-011 | 总结/进度编号列表 → 空数组 |
| TC-DLG-013 | questionFired 标志防重复 | Q-004 | tool_call 渲染后 done 帧跳过 |
| TC-DLG-014 | multiple 属性传递 | Q-006 | tool_call args.multiple=true → QuestionData.multiple=true |
| TC-DLG-015 | buildOutgoingMessage 注入 student_profile | Q-005 | context 含 student_profile 时消息含该字段 |
| TC-DLG-016 | 多卡答案合并提交 | Q-004 | 多卡场景最后一张提交时合并全部答案 |
| TC-DLG-017 | vitest 55 passed | 回归 | 全通过（含 Q-011 的 8 个新测试） |
| TC-DLG-018 | tsc 0 error | 回归 | useStreamingChat.ts + Create.tsx 零错误 |
| TC-DLG-019 | vite build 成功 | 回归 | 构建成功 |
| TC-DLG-020 | 后端 pytest 87 passed | 回归 | 核心 4 文件全通过 |
| TC-DLG-021 | config migrate 通过 | 回归 | "配置已为当前架构版本" |
| TC-DLG-022 | extractChoiceListStrict 状态标记过滤 | Q-011 | ✅/❌ 开头的列表项不被提取 |

### 2.3 WS 真实对话回归（TC-DLG-021~025）

| 编号 | 用例 | 脚本 | 验证问题 | 通过标准 |
|------|------|------|---------|---------|
| TC-DLG-021 | 7 轮连续对话 | ws_regression_test.py | Q-001~Q-007 | 0 问题检出 |
| TC-DLG-022 | ask_question tool_call 率 | ws_regression_test.py | Q-002 | > 85% |
| TC-DLG-023 | 多选 multiple 传递 | ws_regression_test.py | Q-006 | 至少 1 次 multiple=true |
| TC-DLG-024 | 总结请求不产生卡 | ws_regression_test.py | Q-003 | "总结进度"→0 张卡 |
| TC-DLG-025 | 不重复问已答问题 | ws_regression_test.py | Q-005 | 回答年级后不再问年级 |

### 2.4 数据安全与恢复（TC-DATA-001~004，后端单元）

> **2026-07-27 新增**：针对 Q-014/Q-015 脏数据导致 `/projects/{id}/workspace` 500 的问题。验证读取层规范化、写入层防御、以及全表无回归。

| 编号 | 用例 | 验证问题 | 通过标准 |
|------|------|---------|---------|
| TC-DATA-001 | `_normalize_stage_statuses` 规范化所有非法状态 | Q-014 | 输入 `in_progress/not_started/pending/todo/done/finished/unknown`，输出分别映射为 `active/locked/locked/locked/completed/completed/locked`；合法值 `locked/active/completed/skipped` 原样保留 |
| TC-DATA-002 | `_normalize_stage_statuses` 处理 dict 与 str 两种 stage 值格式 | Q-014 | `{"status":"in_progress"}` → `{"status":"active"}`；`"completed"`（旧 str 格式）→ `{"status":"completed","data":{}}` |
| TC-DATA-003 | `json_loads` 多层 JSON 自动解码 | Q-015 | 输入 3 层编码的 `'"{\\"a\\":1}"'`，输出 `{"a":1}`（dict，非 str）；单层/空值仍正确 |
| TC-DATA-004 | `create_skill_state` 写入时规范化 stages（P0-1） | Q-014 | 构造含 `in_progress` 的 SkillState（model_construct 绕过校验）→ create_skill_state → 读回 DB，所有 status 已是合法四态之一 |
| TC-DATA-005 | `SkillStateWriterTool` 解包字符串型 JSON 值（P0-2） | Q-015 | AI 传 `{"light_step_data":"{\\"project_name\\":\\"x\\"}"}` → 工具执行后 DB 中 light_step_data 仅单层编码，解码后是 dict |
| TC-DATA-006 | 全表 workspace 接口无 500（回归） | Q-014+Q-015 | 遍历全部 skill_states，对每个调 `_build_workspace_payload`，失败数 = 0（排除合理的 404「项目不存在」） |
| TC-DATA-007 | 后端 verify-question 接口判断准确 | Q-003 | `POST /chat/verify-question`：番茄钟功能介绍/状态汇报/非疑问句→is_real=false；选轨道/教学模式/年级/兴趣多选→is_real=true |
| TC-DATA-008 | 前端 isLikelyQuestionTitle + isFunctionDescriptionList | Q-003 | 前端单测：列举词标题（包含/功能如下）拒绝；疑问句标题通过；番茄钟选项列表被功能描述启发式拦截 |
| TC-DATA-009 | "具备/特点/特性"不误伤真问题（7-28 修正） | Q-003 | "你想让它具备什么核心功能？选一个"→is_real=true（不再被"具备"误杀）；后端同步 |
| TC-DATA-010 | 开放追问不渲染选项卡（正确行为） | Q-003 | AI 回复"请任选 1-2 个问题回答"+ 列表项 → 不产生 question-card（开放追问走输入框，不是 bug） |
| TC-DATA-011 | 学生画像持久化接口（Q-017） | Q-017 | `POST /student-profile` 保存画像 → `GET /workspace` 读回 `progress.student_profile` 完整一致 |
| TC-DATA-012 | SkillStateReader 返回画像（Q-017） | Q-017 | AI 调 `skill_state_reader(include=['modes'])` → 返回 `student_profile` 字段（AI 能读到历史选择） |
| TC-DATA-013 | `_extract_confirmed_project_name` 多源提取 | Q-022 | 后端单测：① brief_content JSON 字符串含 project_name→提取成功 ② brief_content dict→成功 ③ 顶层 project_name→成功 ④ light_step_data.project_name→成功 ⑤ title 回退→成功 ⑥ 无名字/空白/非法JSON/null→返回 None（共 8+ 反例正例） |
| TC-DATA-014 | `_sync_project_name_from_skill_state` 自动同步 | Q-022 | 后端单测（mock db）：① 确认名≠projects.name→调 update_project 同步 ② 名字已一致→跳过 ③ 确认名是当前名前缀→跳过 ④ 无确认名→跳过 ⑤ update 抛异常→不崩返回原名 ⑥ project=None→返回空串（共 6 用例） |
| TC-DATA-015 | workspace 接口触发项目名自愈（集成） | Q-022 | 构造 projects.name="默认长名字" + skill_state 含 AI 确认名"英语单词助手" → 调 `GET /workspace` → 返回的 `project.name`="英语单词助手" + DB projects.name 已被同步更新 |

---

## 3. 执行顺序

```
0. 前置准备
   - 重启 ZeroClaw daemon（加载最新 prompt）
   - 启动前端 dev server（加载最新前端代码）
   - curl 确认两个服务健康

1. 单元测试（TC-DLG-011~020 + TC-DATA-001~015）
   cd apps/frontend && npx vitest run                                       # 含 questionParser 测试（TC-DATA-008~010, Q-003 前端防线；Q-020 风格词）
   cd apps/frontend && npx tsc --noEmit
   cd apps/frontend && npx vite build
   cd apps/backend && python -m pytest tests/test_mcp_server.py tests/test_stage_constants.py tests/test_tools_gates.py tests/test_check_gate_structural.py -v
   cd apps/backend && python -m pytest tests/test_data_integrity.py -v       # Q-014/Q-015 数据安全（TC-DATA-001~005）
   cd apps/backend && python -m pytest tests/test_question_verifier.py -v    # Q-003/Q-020 后端确认（TC-DATA-007/011~012）
   cd apps/backend && python -m pytest tests/test_project_name_sync.py -v    # Q-022 项目名自愈（TC-DATA-013~014）

2. WS 回归测试（TC-DLG-021~025）
   cd apps/backend
   set PYTHONIOENCODING=utf-8
   python scripts/ws_regression_test.py

3. ⚠️ Playwright 有头 E2E（TC-DLG-001~010 + TC-DLG-026，最核心）
   cd apps/frontend
   set RUN_AI_E2E=1
   npx playwright test --project=chromium --headed --video=retain-on-failure --screenshot=on

4. 全表 workspace 回归（TC-DATA-006）
   cd apps/backend && python scripts/verify_workspace_integrity.py   # 遍历全部项目验证 /workspace 不报 500

5. 问题清单对照
   → 逐项确认 Q-001~Q-015 状态
   → 测试报告中列出每项结果

6. 出测试报告
   → reports/对话系统回归测试报告_<date>.md
```

---

## 4. Playwright spec 要求

测试脚本（`apps/frontend/tests/specs/zeroclaw-integration.spec.ts`）必须：

1. **有头模式**：`--headed` + `--video=retain-on-failure` + `--screenshot=on`
2. **推进到 stage_04**：自动完成 stage_00 三轮 → stage_01 脑爆 → stage_02 开题 → stage_03 范围 → stage_04 轨道
3. **每轮验证**：检查 `question-card` 可见 + 点选项 + 等下一轮
4. **多选验证**：stage_01 的兴趣卡片选 2 个选项
5. **总结误识别验证**：stage_02 发"总结进度"后检查无 `question-card`
6. **截图取证**：每轮 before/after 截图

### 关键 testid

| testid | 元素 | 用途 |
|--------|------|------|
| `chat-input` | 聊天输入框 | 输入消息 |
| `send-button` | 发送按钮 | 点击发送 |
| `question-card` | 选项卡根元素 | 验证卡片出现 |
| `question-option` | 选项按钮 | 点击选项 |
| `message-assistant` | AI 消息 | 验证回复内容 |

---

## 5. 问题清单对照表（测试报告必须填写）

| 编号 | 问题 | TC 用例 | 结果 | 证据 |
|------|------|---------|------|------|
| Q-001 | 选项卡丢失 | TC-DLG-001~002 | ☐✅☐❌ | |
| Q-002 | AI 不调 ask_question | TC-DLG-022 | ☐✅☐❌ | |
| Q-003 | 总结/功能介绍误识别为卡片 | TC-DLG-007, 028, TC-DATA-007~010 | ☐✅☐❌ | |
| Q-004 | 重复选项卡 | TC-DLG-003 | ☐✅☐❌ | |
| Q-005 | 重复问已答问题 | TC-DLG-006, 025 | ☐✅☐❌ | |
| Q-006 | 多选不生效 | TC-DLG-005, 023 | ☐✅☐❌ | |
| Q-007 | 只总结无下一步 | TC-DLG-008 | ☐✅☐❌ | |
| Q-008 | 回复截断 | TC-DLG-003 | ☐✅☐❌ | |
| Q-009 | 思考链不显示 | TC-DLG-010 | ☐✅☐❌ | |
| Q-010 | [选择] 格式失败 | TC-DLG-004 | ☐✅☐❌ | |
| Q-014 | stages 非法状态值致 500 | TC-DLG-026, TC-DATA-001~004, 006 | ☐✅☐❌ | |
| Q-015 | light_step_data 多层编码致 500 | TC-DLG-026, TC-DATA-003, 005, 006 | ☐✅☐❌ | |
| Q-016 | 对话不落库，打开项目空白 | RT-16（刷新后对话恢复） | ☐✅☐❌ | |
| Q-017 | AI 失忆重复问已答问题 | TC-DLG-029, TC-DATA-011, 012 | ☐✅☐❌ | |

---

## 6. 环境准备

### 必须重启 daemon

```bash
# 停旧 daemon
powershell: Stop-Process -Name zeroclaw -Force

# 启动新 daemon（加载最新 config.toml + SKILL.md + SOUL.md）
H:\dev-env\zeroclaw\bin\zeroclaw.exe daemon

# 等待就绪
sleep 5
curl http://127.0.0.1:42617/health
```

### 必须启动前端

```bash
cd apps/frontend && npm run dev
sleep 3
curl http://localhost:5184/
```

### 关键路径

| 路径 | 用途 |
|------|------|
| `H:\dev-env\zeroclaw\bin\zeroclaw.exe` | ZeroClaw CLI |
| `H:\dev-env\zeroclaw\config\config.toml` | 配置（含最新 prompt） |
| `apps/frontend/tests/specs/zeroclaw-integration.spec.ts` | Playwright spec |
| `apps/backend/scripts/ws_regression_test.py` | WS 回归脚本 |
| `.trae/documents/问题清单_长期维护.md` | 问题清单（必读） |

---

**文档结束。测试 agent 执行前必须读本计划 + 问题清单 + 测试工作指南。**
