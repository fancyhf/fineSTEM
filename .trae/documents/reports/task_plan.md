# 任务计划

- 任务名称：创造主链路一次性重构
- created_at: 2026-06-13 15:10:34.000
- 当前状态：进行中

## 目标

- 产出正式解决方案文档，明确“创造”主链路的产品目标、技术方案、数据结构和验收标准。
- 一次性修复当前“创造”主链路的关键断点：状态源分裂、历史恢复不完整、阶段推进不可靠、编辑器/预览接入不稳定。
- 将当前实现从“会聊天的开题页面”提升为“以项目为中心的可恢复工作台”。

## 本次实施范围

1. 新增正式方案文档并归档。
2. 统一 `project` 与 `skill_state` 的运行时状态源到 ORM 仓储。
3. 修复项目代码/聊天记录持久化与恢复接口。
4. 新增完整工作台恢复接口，供创造页与历史项目入口复用。
5. 去除前端阶段硬编码，改为以后端真实状态为准。
6. 补充阶段变更事件，保证创造页可响应真实流程推进。
7. 完成后端编译、前端构建、关键链路回归验证。

## 风险与约束

- 当前工作区已有未提交改动，实施时必须避免覆盖既有用户修改。
- 现有 `Create.tsx` 体量较大，优先做“接口与状态修复”，避免一次性 UI 全量重写导致回归面过大。
- 当前数据库结构已存在历史迁移兼容问题，新增接口需兼容旧数据。

## 执行顺序

1. 落盘解决方案文档与执行记录文件。
2. 改后端仓储和项目接口。
3. 改 Agent 事件与阶段同步。
4. 改前端 `api.ts`、`Create.tsx`、`ProjectDetail.tsx` 恢复链路。
5. 运行诊断、编译、构建与关键回归命令。

## 验收标准

- 新建项目后不再因状态错位长期停留在开题阶段。
- 历史项目重新打开时可恢复项目基本信息、当前阶段、聊天记录、代码内容。
- 阶段进度展示以后端真实状态为准，不再写死 `stage_01` / `stage_07`。
- 代码和聊天保存失败不能再被静默吞掉。
- 前端构建通过，后端编译通过，关键接口可读写。
