# fineSTEM Git 标签管理标准

> **版本**: v1.0.0
> **创建时间**: 2025-12-29 00:00:00.000
> **维护者**: AI Agent
> **状态**: 生效中

---

## 📋 目录

1. [概述](#概述)
2. [标签命名规范](#标签命名规范)
3. [版本号规则](#版本号规则)
4. [发布类型策略](#发布类型策略)
5. [标签管理流程](#标签管理流程)
6. [标签使用示例](#标签使用示例)
7. [常见问题](#常见问题)

---

## 概述

本文档定义了 fineSTEM 项目的 Git 标签管理标准，确保版本控制的规范性和可追溯性。

### 核心原则

- **语义化**: 标签名应清晰表达版本含义
- **可追溯**: 每个标签应有明确的用途说明
- **一致性**: 统一的命名格式和版本策略
- **自动化**: 尽量使用自动化工具管理标签

---

## 标签命名规范

### 标签格式

```
v{MAJOR}.{MINOR}.{PATCH}-{TYPE}-{PHASE}-{STATUS}
```

### 格式说明

| 组件 | 说明 | 示例 |
|------|------|------|
| `v` | 版本前缀，固定为 `v` | `v` |
| `MAJOR` | 主版本号（重大更新） | `1`, `2` |
| `MINOR` | 次版本号（功能增强） | `0`, `1`, `2` |
| `PATCH` | 补丁号（Bug修复） | `0`, `1`, `2` |
| `TYPE` | 发布类型（mvp/beta/rc/final） | `mvp`, `beta`, `rc` |
| `PHASE` | 阶段标识（phase1/phase2/...） | `phase1`, `phase2` |
| `STATUS` | 状态标识（release/stable/canary） | `release`, `stable`, `canary` |

### 完整示例

```
v1.0.0-mvp-phase1-release
```

- **版本**: 1.0.0（第一个主版本）
- **类型**: mvp（最小可行产品）
- **阶段**: phase1（第一阶段）
- **状态**: release（正式发布）

---

## 版本号规则

遵循 [语义化版本规范](https://semver.org/lang/zh-CN/) (Semantic Versioning)

### 版本号结构

#### MAJOR（主版本）

**触发条件:**
- 架构重大重构
- 不兼容的API变更
- 核心技术栈升级
- 数据库结构重大变更

**示例:**
```
v1.0.0 → v2.0.0  # 整体重构
```

#### MINOR（次版本）

**触发条件:**
- 新增功能模块
- 重大功能增强
- 向后兼容的API变更
- 新增支持的平台

**示例:**
```
v1.0.0 → v1.1.0  # 新增用户中心功能
```

#### PATCH（补丁版本）

**触发条件:**
- Bug修复
- 性能优化
- 安全补丁
- 文档更新

**示例:**
```
v1.0.0 → v1.0.1  # 修复登录Bug
```

---

## 发布类型策略

### MVP（最小可行产品）

**使用场景:**
- 项目初期阶段
- 核心功能验证
- 快速迭代发布

**命名示例:**
```
v1.0.0-mvp-phase1-release  # MVP第一阶段发布版
v1.0.1-mvp-phase1-stable  # MVP第一阶段稳定版
```

### Beta（测试版）

**使用场景:**
- 功能开发完成，进入测试阶段
- 需要用户反馈的版本
- 功能验证但不稳定

**命名示例:**
```
v1.1.0-beta-phase2-canary  # 第二阶段金丝雀测试版
```

### RC（候选发布版）

**使用场景:**
- 准备正式发布前的最终测试
- 已知Bug全部修复
- 生产环境预演

**命名示例:**
```
v1.0.0-rc-phase1-stable  # 第一阶段候选发布版
```

### Final/Release（正式版）

**使用场景:**
- 功能完整、稳定可用
- 生产环境部署
- 对外正式发布

**命名示例:**
```
v1.0.0-final-phase1-release  # 第一阶段正式发布版
```

---

## 标签管理流程

### 1. 代码提交

```bash
# 1. 查看当前状态
git status

# 2. 添加修改的文件
git add .

# 3. 提交代码（遵循 Conventional Commits）
git commit -m "feat: add user authentication module"
```

### 提交信息格式

遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型说明:**
- `feat`: 新功能
- `fix`: Bug修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具配置

**示例:**
```
feat(auth): add WeChat login support

- Add WeChat OAuth2 integration
- Support user binding via WeChat OpenID
- Update authentication flow

Closes #123
```

### 2. 创建标签

```bash
# 创建带注释的标签
git tag -a v1.0.0-mvp-phase1-release -m "主支第一阶段MVP发布版

- 完成Track A和Track E前端功能
- 实现FastAPI后端服务
- 集成DeepSeek AI API
- 完成Docker部署配置
- 部署到腾讯云生产环境

版本: v1.0.0
类型: MVP
阶段: Phase 1
状态: Release"
```

### 3. 推送到远程

```bash
# 推送代码
git push origin main

# 推送标签
git push origin v1.0.0-mvp-phase1-release

# 推送所有标签（不推荐）
git push origin --tags
```

### 4. 标签验证

```bash
# 查看所有标签
git tag

# 查看标签详情
git show v1.0.0-mvp-phase1-release

# 查看指定标签的提交
git log v1.0.0-mvp-phase1-release
```

---

## 标签使用示例

### 典型发布场景

#### 场景1: MVP第一阶段发布

```bash
# 提交代码
git commit -m "release: MVP phase1 initial release"

# 创建标签
git tag -a v1.0.0-mvp-phase1-release -m "主支第一阶段MVP发布版"
git push origin main
git push origin v1.0.0-mvp-phase1-release
```

#### 场景2: Bug修复（补丁版本）

```bash
# 修复Bug
git commit -m "fix: resolve login session timeout issue"

# 创建补丁标签
git tag -a v1.0.1-mvp-phase1-release -m "修复登录会话超时问题"
git push origin main
git push origin v1.0.1-mvp-phase1-release
```

#### 场景3: 第二阶段功能增强

```bash
# 新增功能
git commit -m "feat: add learning progress tracking"

# 创建新版本标签
git tag -a v1.1.0-mvp-phase2-beta -m "第二阶段Beta版：新增学习进度跟踪功能"
git push origin main
git push origin v1.1.0-mvp-phase2-beta
```

#### 场景4: 重大重构

```bash
# 完成重构
git commit -m "refactor: migrate to microservices architecture"

# 创建主版本标签
git tag -a v2.0.0-final-phase1-release -m "主版本2.0：微服务架构重构"
git push origin main
git push origin v2.0.0-final-phase1-release
```

### 版本演进示例

```
v1.0.0-mvp-phase1-release     # MVP第一阶段发布
  ↓ (Bug修复)
v1.0.1-mvp-phase1-release     # 补丁修复
  ↓ (功能增强)
v1.1.0-mvp-phase2-beta        # 第二阶段Beta
  ↓ (测试稳定)
v1.1.0-rc-phase2-stable       # 第二阶段候选发布
  ↓ (正式发布)
v1.1.0-final-phase2-release   # 第二阶段正式版
  ↓ (重大重构)
v2.0.0-final-phase1-release   # 主版本2.0
```

---

## 常见问题

### Q1: 如何删除标签？

```bash
# 删除本地标签
git tag -d v1.0.0-mvp-phase1-release

# 删除远程标签
git push origin :refs/tags/v1.0.0-mvp-phase1-release
```

### Q2: 如何修改标签？

标签创建后不能直接修改，需要删除后重新创建：

```bash
# 删除旧标签
git tag -d v1.0.0-mvp-phase1-release
git push origin :refs/tags/v1.0.0-mvp-phase1-release

# 创建新标签
git tag -a v1.0.0-mvp-phase1-release -m "新的标签说明"
git push origin v1.0.0-mvp-phase1-release
```

### Q3: 是否需要推送所有标签？

**不推荐**。只推送有意义的发布标签：

```bash
# ❌ 不推荐：推送所有标签
git push origin --tags

# ✅ 推荐：只推送指定的标签
git push origin v1.0.0-mvp-phase1-release
```

### Q4: 轻量标签和注释标签有什么区别？

```bash
# 轻量标签（不推荐用于发布）
git tag v1.0.0

# 注释标签（推荐用于发布）
git tag -a v1.0.0-mvp-phase1-release -m "标签说明"
```

**区别:**
- 轻量标签：只是某个commit的引用，不包含额外信息
- 注释标签：完整的Git对象，包含标签信息、创建时间、说明等

**建议**: 发布版本使用注释标签，临时标记可以使用轻量标签

### Q5: 如何基于标签创建分支？

```bash
# 基于标签创建修复分支
git checkout -b hotfix/v1.0.1 v1.0.0-mvp-phase1-release

# 完成修复后，创建新标签
git tag -a v1.0.1-mvp-phase1-release -m "Bug修复"
```

---

## 附录

### A. 快速参考卡片

```bash
# 创建标签
git tag -a <TAG_NAME> -m "<MESSAGE>"

# 推送标签
git push origin <TAG_NAME>

# 查看标签
git tag
git show <TAG_NAME>

# 删除标签
git tag -d <TAG_NAME>                    # 本地
git push origin :refs/tags/<TAG_NAME>   # 远程
```

### B. 标签检查清单

在创建发布标签前，请确认：

- [ ] 所有代码已提交
- [ ] 测试全部通过
- [ ] 文档已更新
- [ ] CHANGELOG已更新
- [ ] 部署到测试环境验证
- [ ] 代码审查通过
- [ ] 标签命名符合规范
- [ ] 标签说明清晰完整

### C. 相关文档

- [Semantic Versioning](https://semver.org/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Git 官方文档 - Tagging](https://git-scm.com/book/en/v2/Git-Basics-Tagging)
- [fineSTEM 项目规范](../.trae/rules/project_rules.md)
- [部署文档](../DEPLOYMENT.md)

---

## 变更记录

| 版本 | 日期 | 变更内容 | 维护者 |
|------|------|----------|--------|
| v1.0.0 | 2025-12-29 | 初始版本，定义标签管理标准 | AI Agent |
